# Аудит очистки кода

**Дата аудита:** 2026-07-22

**Целевая ветка:** `staging`

**Цель:** найти артефакты, которые можно удалить, и ограниченные упрощения, не меняя поведение во время работы.

**Статус реализации:** находки с высокой уверенностью и низким риском выполнены в рамках той же очистки.

## Итоги реализации

Сделано:

- удалены четыре недостижимых модуля исходного кода, устаревший сборщик sidecar, средство запуска тестов без тестов и завершенные описания задач;
- удален буфер отладочного журнала, который существовал только для недостижимой панели отладки, при этом диагностика в консоли браузера сохранена;
- закрыты все 60 находок неиспользуемого кода, подтвержденных компилятором, и включены проверки неиспользуемого кода в клиенте и на сервере;
- удалены 53 невостребованных клиентских хука, вспомогательных функции, типа и объявления интерфейса – пакетами по предметным областям;
- удалены восемь осиротевших зависимостей с высокой уверенностью, а также исправлены файл блокировки, проверка установки рабочего пространства и текст по устранению неполадок;
- корневая команда `pnpm test` теперь запускает реальные регрессионные проверки, а не сообщает об успехе при нуле тестов;
- переиспользован существующий выбор ключевого кадра раскадровки, а дублированная логика токенов запроса Spotify объединена;
- переупорядочивание переменных пресета ограничено запрошенным пресетом: ранее игнорировавшийся `presetId` теперь работает как граница целостности.

Сознательно оставлено для отдельной работы по совместимости или продукту:

- `@rollup/wasm-node` и `Mari_point_down_left.png`;
- серверные экспорты, которые могут быть внешними API или точками подключения тестов;
- объединение парсера PNG и геометрии обучающих подсказок;
- крупные переработки редакторов, поля ввода и больших модулей;
- поля совместимости, запланированные к следующему мажорному выпуску.

Подробные находки ниже сохранены как свидетельства состояния до изменений. Там, где остались формулировки в виде рекомендаций, приоритет имеет этот раздел об итогах реализации.

## Проверка

Выполненная очистка прошла все поддерживаемые в репозитории способы проверки:

- `pnpm install --frozen-lockfile`
- `pnpm check` (контроль неиспользуемого кода, TypeScript, ESLint и продакшен-сборки)
- `pnpm test` (все регрессионные наборы плюс дымовые проверки в браузере: 81 пройден, 51 пропущен намеренно)

Браузерный набор попутно вскрыл четыре предположения о поиске элементов, зависевшие от состояния, – это выяснилось, когда общая команда тестов стала честной. Теперь эти тесты переходят к нужному экрану явно, разграничивают дублирующиеся мобильные элементы управления и обращаются к настоящему прокручиваемому блоку ленты Noodle, не ослабляя при этом свои продуктовые проверки.

## Краткая сводка

Репозиторий большой (1665 отслеживаемых файлов и примерно 478 000 строк по рассмотренным типам файлов с исходным кодом), однако большинство крупных файлов – это работающий код продукта, а не очевидный мусор. Самый безопасный путь очистки – набор небольших удалений, каждое со своим доказательством, а не масштабная переработка.

Первая линия очистки в исходном аудите выявила:

- четыре модуля исходного кода, на которые никто не ссылается (899 строк суммарно);
- один устаревший скрипт сборки sidecar (173 строки);
- одно средство запуска тестов, которое завершается успешно, не выполнив ни одного теста (54 строки плюс привязка в скриптах пакета);
- два завершенных описания задач по этапам, оставшихся в корне репозитория (235 строк);
- 60 неиспользуемых объявлений, импортов, параметров и локальных переменных, подтвержденных компилятором;
- восемь вероятно осиротевших прямых зависимостей – при условии проверки чистой установкой и сборкой;
- один вероятно неиспользуемый статический спрайт Mari – после дымовой проверки в браузере.

Только четыре недостижимых модуля, устаревший скрипт, ничего не делающее средство запуска тестов и описания задач дают 1361 отслеживаемую строку. Предложенную работу все же стоит разбить на небольшие PR по очистке, чтобы у каждого удаления было узкое доказательство и простой откат.

## Как проводился аудит

Аудит опирался на несколько видов свидетельств:

1. Инвентаризация всех отслеживаемых файлов, типов файлов, основных областей исходного кода и самых больших файлов.
2. Анализ импортов и экспортов по дереву TypeScript (AST), включая относительные импорты и псевдонимы репозитория.
3. Поиск точных имен и названий файлов по всему отслеживаемому исходному коду, скриптам, документации, манифестам и рабочим процессам.
4. Пробные запуски компилятора TypeScript с принудительно включенными `noUnusedLocals` и `noUnusedParameters` для клиента и сервера.
5. Поиск по прямым зависимостям плюс точечный разбор истории Git там, где зависимость или скрипт выглядели брошенными после прежней переработки.
6. Сравнение нормализованных окон текста на дубликаты, а затем ручной разбор самых заметных совпадений.
7. Проверка синтаксиса отслеживаемых файлов JSON, Python и Bash.

Метки уверенности, которые используются ниже:

- **Высокая:** несколько независимых проверок сходятся; удаление должно быть механическим.
- **Средняя:** ссылок сейчас нет, но динамическая загрузка, внешние потребители или продуктовый замысел все же могут иметь значение.
- **Отложить:** законная возможность упрощения, но область возможных регрессий слишком широка для прохода по удалению артефактов.

Статический анализ не может доказать, что нигде нет поиска по строке во время работы, использования скачанного пакета, путей, заданных пользователем, или внешних потребителей. Такие случаи отмечены отдельно, а не отнесены к мертвому коду.

## 1. Удаление файлов с высокой уверенностью

### 1.1 Недостижимые модули исходного кода

| Кандидат | Свидетельства | Примечание по очистке | Требуемое доказательство |
| --- | --- | --- | --- |
| `packages/client/src/components/agents/AgentDebugPanel.tsx` (296 строк) | Никто не импортирует, а `AgentDebugPanel` встречается только в собственном объявлении. | Удалить компонент. Затем проверить `debugLog` и `clearDebugLog` в хранилище агентов: кроме этой недостижимой панели их больше никто не использует. Не удаляйте `lastResults` – он нужен `SpriteOverlay`. | `pnpm check`; откройте настройки агентов и режим отладки, проверьте работающие отладочные экраны. |
| `packages/client/src/components/agents/AgentThoughtBubbles.tsx` (113 строк) | Никто не импортирует, а `AgentThoughtBubbles` встречается только в собственном объявлении. Текущий интерфейс облачков мыслей и списка проверок рисуют `RoleplayHUD` и `RoleplayHUDActionsMenu`. | Удалить компонент и устаревшую запись о нем в `packages/client/.instructions.md`. | `pnpm check`; `pnpm regression:roleplay`; проверьте в браузере панель HUD режима Roleplay и список проверок непрерывности. |
| `packages/client/src/components/panels/GlobalGalleryPanel.tsx` (468 строк) | Нет ни импортов, ни регистрации маршрута, ни упоминаний точного имени. | Удалить только эту панель. **Не** делайте вывод, что вся галерея мертва: `NoodleHome`, хуки галереи, серверные маршруты и хранилище по-прежнему используются. | `pnpm check`; `pnpm smoke:ui`; вручную проверьте загрузку изображений и работу галереи в разделе Noodle. |
| `packages/shared/src/features/turn-games/engine-utils.ts` (22 строки) | Нет импортов, нет экспорта через сводный файл, и все четыре экспортируемых имени встречаются только в этом файле. | Удалить файл. | `pnpm check`; `pnpm regression`. |

### 1.2 Устаревший скрипт сборки sidecar

На `scripts/build-sidecar-runtime.mjs` не ссылаются ни скрипты пакета, ни рабочие процессы, ни документация, ни исходный код. Скрипт вызывает `pnpm exec node-llama-cpp`, но `node-llama-cpp` больше не входит в зависимости рабочего пространства. История Git связывает его с прежним путем сборки локального sidecar для Gemma.

**Рекомендация (высокая уверенность):** удалить скрипт. Перед этим стоит последний раз поискать артефакты выпуска вне репозитория – на случай, если какой-то конвейер установщика настроен снаружи.

### 1.3 Завершенные описания реализации в корне

`MARI_PHASE2_TASK.md` и `MARI_PHASE3_TASK.md` – это инструкции по реализации для отдельных ветвей, и эта работа уже есть в коде. Ничто в репозитории на них не ссылается, и это не долговременная документация для пользователей или участников проекта.

**Рекомендация (высокая уверенность):** убрать их из рабочего дерева. История остается доступной в Git. Если какое-то обоснование все еще ценно, перенесите только это обоснование в подходящий документ по архитектуре, а не сохраняйте инструкции по задачам.

### 1.4 Обманчивое средство запуска тестов без тестов

`packages/server/scripts/run-tests.mjs` нацелен на три шаблона `.test.ts`, но ни в одной из указанных папок нет файла с тестами. И `pnpm --filter @marinara-engine/server test`, и корневая команда `pnpm test` завершаются успешно при нуле тестов и нуле наборов. Прежние тесты были удалены намеренно, а правила репозитория запрещают хранить файлы `.test.ts`.

Это опаснее обычного мертвого кода: зеленый результат `pnpm test` сейчас подразумевает покрытие, которого нет.

**Рекомендация (высокая уверенность):**

1. Удалить серверное средство запуска тестов и серверный скрипт `test`.
2. Оставить проверку структуры установщика для Windows, но при необходимости дать ей честное собственное имя.
3. Переопределить корневой `test` так, чтобы он запускал осмысленный набор регрессионных и дымовых проверок, либо убрать общий псевдоним и описать в документации `pnpm check`, `pnpm regression:*` и `pnpm smoke:ui` как настоящие команды проверки.
4. Исключить возможность того, что CI сообщит "tests passed" только на основании запуска без тестов.

## 2. Очистка зависимостей

У этих прямых зависимостей нет ни импортов, ни регистрации, ни настройки, ни упоминаний строкой во время работы – кроме манифестов и файла блокировки, если не указано иное.

| Рабочее пространство | Зависимость | Уверенность и свидетельства |
| --- | --- | --- |
| client | `class-variance-authority` | **Высокая.** Не используется ни в коде, ни в настройках. Прежняя история очистки зависимостей тоже считала ее неиспользуемой. |
| client | `autoprefixer` | **Высокая, с доказательством сборкой.** Нет ни настройки PostCSS, ни импорта; клиент использует плагин Tailwind для Vite. |
| server | `@earendil-works/pi-ai` | **Высокая.** Среда выполнения Professor Mari была переработана и больше не зависит от Pi. История репозитория прямо фиксирует, что зависимость уже не импортируется и оставлена до отдельной очистки. |
| server | `@fastify/websocket` | **Высокая.** Нет ни регистрации плагина, ни маршрута websocket, ни импорта. |
| server | `png-chunk-text` | **Высокая.** Нет импорта. Работа с метаданными PNG сейчас реализована напрямую. |
| server | `png-chunks-encode` | **Высокая.** Нет импорта. |
| server | `png-chunks-extract` | **Высокая.** Нет импорта. |
| shared | `chess.js` | **Высокая, с доказательством совместимости.** Сейчас в коде не импортируется. Встроенные шахматы вынесены в отдельные необязательные пакеты. При удалении нужно также убрать запись о зависимости в `scripts/check-workspace-install.mjs` и обновить устаревший текст по устранению неполадок про отсутствующий `chess.js`. |

На `@rollup/wasm-node` в клиенте тоже нет ссылок, но это может быть резервный вариант Rollup для отдельных окружений. Считайте эту находку **средней уверенности**: изучите историю упаковки и CI, а перед удалением докажите, что сборка проходит на всех поддерживаемых платформах.

Не относите к неиспользуемым такие зависимости, как `workbox-window`, `pino-pretty`, корневой `esbuild`, пакеты типов или инструменты только для командной строки, лишь потому, что в коде не нашлось текста импорта. Их подключают сгенерированные модули, настройки транспорта, заданные строкой, скрипты сборки или скрипты пакета.

Для PR по зависимостям обновите `pnpm-lock.yaml`, установите все с чистого состояния и прогоните полную линию сборки и проверок. Удаление пакета из уже заполненного дерева `node_modules` доказательством не считается.

## 3. Неиспользуемый код, подтвержденный компилятором

Принудительно включенные проверки неиспользуемого кода в TypeScript дали **57 диагностик по серверу** и **3 по клиенту**. Это более веское свидетельство, чем кандидаты, найденные только текстовым поиском. Большинство – это импорты и локальные переменные, их можно убрать механически; а вот для параметров обратных вызовов и параметров публичных методов сначала нужно проверить сигнатуры вызовов.

### 3.1 Клиент

- `ChatSettingsDrawer.tsx`: неиспользуемый параметр фильтра `subject`.
- `GameCombatUI.tsx`: неиспользуемый параметр `line` в `map`.
- `hooks/use-encounter.ts`: неиспользуемый `_res`; дождитесь запроса, не присваивая результат.

### 3.2 Сервер

- `db/file-backed-store.ts`: неиспользуемый `TABLES_REVERSE`; неиспользуемое поле экземпляра `loadedManifest` и присваивание ему.
- Импорты и локальные переменные в маршрутах: `backup.routes.ts` (`dirname`), `sprites.routes.ts` (`readdir`), `scene.routes.ts` (`gsStorage`), `noodle.routes.ts` (`extractNoodleMentionHandles`, `NoodleInteractionType`) и `generate/dry-run-route.ts` (`lorebooksStore`).
- Неиспользуемые параметры обратных вызовов маршрутов: `game-assets.routes.ts`, `lorebooks.routes.ts`, `sprites.routes.ts` и `youtube.routes.ts` (`reply`). Переименовывайте в `_reply` только если нужно сохранить позицию в сигнатуре Fastify.
- `game.routes.ts`: `GmPromptContext`, `formatMoraleContext` и `sceneSpotifyTrackCandidateSchema`.
- `generate.routes.ts`: `readFileSync`, `LIMITS`, `AgentPhase`, `CharacterStat`, `GameState`, `createLLMProvider`, `formatZonedConversationDate`, `formatZonedConversationTime`, `chatsTable`, `normalizeCustomEmojiSelection`, `embedMemoryRecallTexts`, `latestHistoryUserContent`, `getActiveTurnGame`, `startTurnGame`, `pruneEmptyPromptWrappers`, `areConversationSchedulesEnabled`, `addEventEntry`, `normalizeAgentMaxTokens`, `resolveAgentRunInterval` и локальная переменная `chatParams`.
- `generate/dry-run-route.ts`: мертвая локальная вспомогательная функция `wrapperMessages`.
- `services/agents/agent-executor.ts`: неиспользуемый параметр `agentType` в `sanitizeTextAgentResponse`; при его удалении поправьте внутренние вызовы.
- `services/agents/agent-pipeline.ts`: неиспользуемый `AgentPhase`.
- `services/conversation/schedule.service.ts`: неиспользуемые `createLLMProvider` и `ConversationStatusOverride`.
- `services/game/perception.service.ts`: неиспользуемый `RPGAttributes`.
- `services/generation/conversation-react-command-runtime.ts`: неиспользуемый параметр `command` вспомогательной функции.
- `services/import/st-bulk.importer.ts`: неиспользуемый `personasTable`.
- `services/lorebook/keyword-scanner.ts`: неиспользуемый `currentMessageIndex`, полученный при разборе объекта; перед удалением проверьте форму внутренних параметров.
- `services/lorebook/prompt-injector.ts`: неиспользуемый `LorebookEntry`.
- `services/mari-db/mari-db.service.ts`: мертвая вспомогательная функция `makeEmptyValidation`.
- `services/prompt/assembler.ts`: неиспользуемые `PromptPreset`, `PromptSection`, `PromptGroup`, `groupOrder` и `chatHistoryEndIdx`.
- `services/sidecar/scene-analyzer.ts`: мертвые вспомогательные функции `widgetUpdateHint` и `widgetStateSummary`.
- `services/sidecar/scene-postprocess.ts`: мертвая вспомогательная функция `normalizeExpression`.
- `services/sidecar/sidecar-process.service.ts`: `lastReadyAt` присваивается, но никогда не читается.
- `services/storage/noodle.storage.ts`: неиспользуемый `NoodlerStageProfile`.
- `services/storage/prompts.storage.ts`: неиспользуемый параметр `presetId` в `reorderVariables`; перед изменением сигнатуры проверьте вызовы и то, как хранилище задает порядок.

Когда этот список закрыт, включите `noUnusedLocals` и `noUnusedParameters` в настройках TypeScript для сервера и клиента. Так аудит превращается из разового прохода в постоянно поддерживаемое правило. Параметры обратных вызовов, которые нужны по сигнатуре, лучше помечать префиксом `_`, чем снова отключать правило целиком.

## 4. Внутренние экспорты, которых никто не использует в репозитории

Экспортируемые объявления не попадают под обычную проверку неиспользуемых локальных имен, поэтому второй проход искал имена, которые встречаются только в собственном объявлении. Клиент – это приложение, а не публичная библиотека, поэтому такие имена – хорошие кандидаты на удаление. Удаляйте их пакетами по предметным областям и дайте компилятору вскрыть связанные приватные вспомогательные функции и импорты.

### 4.1 Хуки и вспомогательные функции клиента

- Хуки агентов: `useAgentConfig`, `useUpdateAgentByType`, `useToggleAgent`.
- Хуки персонажей: `useUpdatePersonaGalleryClipTrim`, `useCharacterGroup`.
- Хуки чатов и папок: `useReorderChats`, `useActiveChatPreset`, `useCreateChatPreset`, `useTouchChat`, `useMarkAutonomousUnread`, `useBulkSetMessagesHiddenFromAI`, `useSwipes`, `useMoveConnection`.
- Хуки режима Game Mode: `useRegeneratePartyCard`, `useUpdateGameMapBinding`, `useCombatLoot`, `useLootGenerate`, `useGameJournal`, `useGameCheckpoints`, `useCreateCheckpoint`, `useLoadCheckpoint`, `useDeleteCheckpoint`.
- Хуки Haptic Feedback: `useHapticStopScan`, `useHapticCommand`, `useHapticStopAll`.
- Хуки лорбуков: `useLorebookEntry`, `useBulkCreateEntries`, `useSearchLorebookEntries`.
- Прочие хуки: `useCustomTool`, `useUpdateNoodleAccount`, `usePreset`, `useCreatePreset`, `usePresetGroups`, `useReorderGroups`, `usePresetSections`, `usePresetVariables`, `usePreviewPreset`, `useRegexScript`, `useUpdateSpatialContext`.
- Объявления интерфейса: `parseQteTag`, `NoodlerNotificationItem`, `LabelWithHelp`, `RESOURCE_PANEL_SORT_OPTIONS` и `SyncedSettings`.
- Вспомогательные функции библиотек: `isManagedChatBackgroundUrl`, `isBrowserSpeechRecognitionSupported`, `requestTurnGameBotGeneration`, `resolveInputMacrosForChat`, `createCustomToolFolderPackageFilename`, `resolveCurrentGameSessionChatId`, `readTextFileFromZip` и `buildTTSMessageText`.

Неиспользуемый клиентский хук **не** доказывает, что его серверная точка входа тоже не нужна. Сначала удалите хук; маршруты проверяйте отдельно – по интерфейсу, пакетам возможностей и совместимости с внешним API.

### 4.2 Серверные кандидаты, по которым нужно окончательное решение об API или точках подключения тестов

У перечисленных ниже экспортируемых серверных объявлений тоже нет потребителей внутри репозитория. Большинство выглядит внутренними, но экспортированные точки подключения тестов и вспомогательные функции могут использоваться внешними инструментами, поэтому уверенность остается средней, пока сопровождающие не подтвердят, что это не поддерживаемые API:

- среда выполнения и базовая аутентификация: `getServerRoot`, `getSpotifyRedirectUri`, `isAutoOpenBrowserDisabled`, `hasBasicAuthConfigured`;
- точки подключения тестов: `resetRateLimitBucketsForTests`, `buildKnowledgeRetrievalAgentMessagesForTest`, `splitRuntimeHandledAgentInjectionsForTest`, `__setSdkForTesting`;
- вспомогательные функции генерации и промптов: `normalizeSecretPlotSceneDirections`, `buildUserMessageRegenerationPrompt`, `buildUserMessageRegenerationSourceMessage`, `wrapFields`, `mergeTruncation`, `modelAccessOptions`, `isStandaloneCharacterProfileBlock`, `resolveChatSummaryPromptFromMetadata`;
- вспомогательные функции режима Game Mode: `buildNpcPortraitImagePrompt`, `buildBackgroundImagePrompt`, `buildSceneIllustrationImagePrompt`, `buildSessionSummaryPrompt`, `buildCardAdjustmentPrompt`, `moraleDiceModifier`, `buildNpcRelationshipSummary`, `buildSessionCarryoverContext`, `getTurnGameContextText`;
- вспомогательные функции лорбуков: `enforceMaxActivatedEntries`, `applyPerLorebookTokenBudgets`, `resolveActivatedLorebookEntryContent`, `resolveBudgetAndRecursivelyActivateLorebookEntries`, `recursiveScan`;
- утилиты и типы: `AgentPipelineResult`, `resolveVideoRequestDuration`, `newTimeSortableId`, `parseBoolean`, `sanitizePathFilename`.

Не применяйте эту проверку на "единственное текстовое вхождение" ко всему `packages/shared`: общие экспорты – это обязательства по совместимости для клиента, сервера и скачиваемых пакетов агентов, включая потребителей за пределами этого репозитория.

## 5. Кандидат среди статических ресурсов

`packages/client/public/sprites/mari/Mari_point_down_left.png` – единственный поставляемый спрайт Mari, на имя и путь которого нет ни одной ссылки в репозитории. На соседние ресурсы Mari ссылки есть.

**Рекомендация (средняя уверенность):** проверьте, что ни одно правило формирования имен во время работы и ни одна сторонняя тема оформления не обращаются к файлу напрямую, затем удалите его и просмотрите в браузере все обучающие и вводные позы Mari. Публичные ресурсы могут загружаться по собранным на ходу адресам URL, поэтому одного отсутствия текстовых упоминаний для высокой уверенности недостаточно.

Не используйте поиск по имени файла для чистки поставляемых ресурсов режима Game Mode. Серверные наполнители данных и манифесты просматривают некоторые папки с ресурсами динамически.

## 6. Ограниченные упрощения

Это улучшения удобства поддержки, а не удаление мертвого кода. Каждое должно в точности сохранять поведение и иметь узкое регрессионное доказательство.

### 6.1 Точно или почти точно продублированная бизнес-логика

1. **Выбор ключевого кадра раскадровки – низкий риск.** В `GameSurface.tsx` есть локальная реализация `findStoryboardKeyframeForSegment`, совпадающая с экспортируемой `findReplayStoryboardKeyframe` в `lib/game-session-replay.ts`. Переиспользуйте функцию из библиотеки и удалите локальную копию.
2. **Нормализация поиска Spotify – низкий или средний риск.** `SPOTIFY_STOP_WORDS`, `SPOTIFY_MOOD_EXPANSIONS` и сам процесс расширения дублируются в `game-spotify-music.service.ts` и `tool-executor.ts`. Вынесите небольшую вспомогательную функцию для токенов запроса Spotify, чтобы два пути не могли разойтись.
3. **Извлечение метаданных карточки персонажа из PNG – средний риск.** `extractCharaFromPng` реализован независимо в `import.routes.ts` и `st-bulk.importer.ts`. Вынесите одну серверную утилиту и проверьте на регрессионных примерах обычные текстовые блоки, международные текстовые блоки, данные в base64 и в сыром виде, карточки V2 и V3, а также испорченные файлы PNG.
4. **Геометрия всплывающих подсказок в обучении – средний риск.** `GameTutorial.tsx` и `OnboardingTutorial.tsx` дублируют логику пересечений и размещения. Вынесите только общий расчет геометрии; правила для мобильных устройств и продуктовые особенности каждого обучения оставьте явными параметрами.
5. **Нормализация правки игрового фрагмента на клиенте и сервере – средний или высокий риск.** Чистая нормализация в клиенте и на сервере похожа. В общий код переносите только то, что действительно не зависит от среды выполнения – схему или нормализатор; разбор данных и сохранение оставьте на сервере.

### 6.2 Большие повторяющиеся области интерфейса: широкое объединение отложить

- `CharacterEditor.tsx` и `PersonaEditor.tsx` содержат заметно повторяющийся процесс управления спрайтами.
- `ChatInput.tsx` и `ConversationInput.tsx` повторяют логику направляемого плана и поля ввода.

Смысл в объединении действительно есть, но слияние любой пары целиком создало бы огромную область возможных регрессий. Выносите по одному целостному хуку или компоненту за раз – для редакторов начните с управления спрайтами, для полей ввода с направляемого плана – и после каждого выноса проверяйте в браузере оба места использования.

### 6.3 Работающие точки повышенной сложности

Самые большие работающие модули – это `server/routes/game.routes.ts`, `client/components/game/GameSurface.tsx`, `client/components/chat/ChatSettingsDrawer.tsx`, `server/routes/generate.routes.ts` и `client/components/panels/SettingsPanel.tsx`. Удалять их не нужно. Продолжайте выносить отдельные обработчики маршрутов, службы предметных областей, разделы выдвижной панели и чистые вспомогательные функции – но только когда затронутую функцию и так меняют. Отдельный PR в духе "разделить все" добавил бы суеты без надежного доказательства сохранения поведения.

## 7. Что сознательно исключено из очистки

- Поля совместимости, явно принятые на весь ряд выпусков 2.x: формы совместимости для стиля изображений, состояния игры, синтеза речи, трекера персон и контекста режима Conversation. Удалять их можно только через миграцию с изменением версии в следующем мажорном выпуске.
- Сгенерированные реестры и манифесты возможностей. Пересоздавайте их своими скриптами, не правьте вручную.
- Код скачиваемых пакетов агентов Illustrator, Music DJ, Lorebook Keeper и остальных. Очистка среды выполнения и промптов, принадлежащих агентам, относится к `Pasta-Devs/Marinara-Agents`; здесь остается только их встраивание в приложение.
- Модули Home Assistant в `custom_components` – их обнаружение построено на соглашениях об именах и манифестах.
- `MarinaraLauncher.exe` – его использует код миграции ярлыков на панели задач.
- `start-local.bat` – на него не ссылаются скрипты пакета, но он вполне может служить человеку для локального запуска. Удаляйте только после уточнения замысла у сопровождающих.
- Объявления схем, которые выглядят неиспользуемыми, но выполняются при инициализации модуля или регистрации таблиц.
- Серверные маршруты – только потому, что не используется удобный хук React. Их все еще могут вызывать скачиваемые пакеты или потребители API.

## 8. Рекомендуемый порядок очистки

Работа должна остаться простой и удобной для проверки:

1. **PR A – артефакты:** удалить четыре недостижимых модуля, устаревшую запись о компоненте в документации, устаревший скрипт sidecar, завершенные описания задач и – после ручного подтверждения – неиспользуемый спрайт Mari.
2. **PR B – честная картина тестов:** удалить средство запуска тестов без тестов и переименовать или переопределить скрипты пакета так, чтобы успешные команды означали настоящие проверки.
3. **PR C – очистка по компилятору:** закрыть 60 диагностик TypeScript, затем включить проверки неиспользуемого кода в настройках клиента и сервера.
4. **PR D – зависимости:** удалить восемь пакетов с высокой уверенностью, исправить проверку установки рабочего пространства и текст по устранению неполадок, пересоздать файл блокировки и доказать, что чистая установка и сборка проходят.
5. **PR E и далее – пакеты по областям:** удалять неиспользуемые клиентские экспорты по предметным областям, а затем по одному разбирать дубликаты вспомогательных функций с низким риском.

Не смешивайте в одном PR по очистке удаление зависимостей, широкую переработку интерфейса и разбор маршрутов.

## 9. Таблица проверок

Для каждого изменения выполняйте подходящую проверку:

- Любая очистка кода: `pnpm check`.
- Изменения в общем коде или крупные серверные: сначала `pnpm regression` либо узкая команда `pnpm regression:<domain>`, а перед слиянием – полный набор.
- Очистка компонентов и хуков интерфейса: `pnpm smoke:ui` плюс ручная проверка затронутого сценария в браузере.
- Пути промптов, агентов или режима Roleplay: `pnpm regression:prompt` и/или `pnpm regression:roleplay`.
- Очистка зависимостей: чистая установка по файлу блокировки, `pnpm check`, продакшен-сборки и CI на поддерживаемых платформах.
- Объединение импорта PNG: прямые регрессионные проверки импорта на корректных и испорченных карточках персонажей.
- Файлы выпуска и версий, если они неожиданно затронуты: `pnpm version:check` и `pnpm credits:check`.

До этой очистки результат общей команды `pnpm test` нельзя было приводить как доказательство тестирования, потому что она завершалась успешно, не выполнив ни одного теста.

## 10. Проверка аудита и его ограничения

В ходе аудита:

- все отслеживаемые файлы JSON разобраны успешно;
- все 12 отслеживаемых файлов Python разобраны успешно средствами разбора синтаксиса Python;
- `start.sh`, `start-termux.sh` и `android/build-apk.sh` прошли `bash -n`;
- пробные запуски TypeScript на неиспользуемый код дали 57 серверных и 3 клиентских находки, описанные выше;
- успешное завершение серверной и корневой команд тестов при нуле тестов наблюдалось напрямую.

ShellCheck и PowerShell не были установлены, поэтому смысловая проверка shell-скриптов и разбор скриптов PowerShell и Windows не выполнялись. Цели Android и Home Assistant осмотрены по структуре, но полностью в этом аудите не собирались. Такие проверки платформ относятся к тем PR по очистке, которые затрагивают их файлы.
